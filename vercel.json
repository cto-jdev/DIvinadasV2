{
    "buildCommand": "cd apps/web && pnpm build",
    "outputDirectory": "apps/web/.next",
    "installCommand": "npx --yes pnpm@10 install",
    "crons": [
        {
            "path": "/api/cron/refresh-tokens",
            "schedule": "0 3 * * *"
        },
        {
            "path": "/api/cron/purge-expired",
            "schedule": "0 4 * * 0"
        }
    ]
}
